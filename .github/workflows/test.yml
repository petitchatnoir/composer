name: Get
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  Get Vault Secret:
    container: 'adeo-docker-adeo-software-factory-release.jfrog.io/vault/vault-ci:latest'
    
    steps:
        - name: Build vault
          run: ./build
